<script type="text/javascript">

</script>
<div class="row">	
	<h1>Welcome, <%= @user.username %></h1>
	E-mail: <%= @user.email %><br />
	<%= link_to 'edit your profile', edit_user_path %>
	<hr>

</div>
<div class="row">

	<!-- col-offset to main the distance between 2 columns, push make article stay at the right side in when screen in higher resolution  -->
	<article class="col-sm-7 col-sm-offset-1 col-sm-push-4">
		<div class="table-responsive">
			
				<table class="table" >
					<thead>
						<tr><th>name</th><th>options</th><th></th></tr>
					</thead>

					<tbody id="employees">
					<% if @employees.size > 1 %>
						<%= render @employees  %>
					<% else %>
						<tr id="no_employee_text"><td><p calss="label">no employees available<td><tr><p>
					<% end %>
					</tbody>
				</table>
		<div>
	</article>
	<!-- pull to keep the aside on the left, corresponding to article push, in higher resolution -->
	<aside class="col-sm-4 col-sm-pull-8">
		<%= link_to 'add employees', new_user_employee_path(@user), id: "new_employee_link", remote: true %>
		<br />
		
	</aside>
</div>





